# Dev (npm run dev): leave empty so requests go to the same origin and Vite proxies /api → gateway.
# VITE_API_BASE=

# Only if you serve the built SPA without nginx/Vite proxy (e.g. file:// or wrong port):
# VITE_API_BASE=http://localhost:8080
